<h1>Listing gossips</h1>

<table>
  <tr>
    <th>Text</th>
    <th>Score</th>
    <th>User</th>
    <th>Action</th>
  </tr>

  <% @gossips.each do |gossip| %>
  <tr>
    <td><%= gossip['text'] %></td>
    <td></td>
    <td></td>
    <td><a href="/gossips/<%= gossip['id'] %>"><button class="btn">Read more</button></a></td>
  </tr>

  <% end %>
</table>

<br />

<%= link_to 'New Gossip', new_gossip_path %>
